from kivy.app import App
from kivy.uix.image import Image,AsyncImage
from kivy.uix.carousel import Carousel
from kivy.animation import Animation
from kivy.uix.button import Button

class PitonApp(App):
    def c_animation(self,instance):
        animation=Animation(pos=(200,0))
        animation.start(self.img)
    def build(self):
        #pass
        #carousel = Carousel(direction='right')
        #for i in range(10):
            #src = "figure-up.gif","figure-down.gif","figure-right.gif","figure-left.gif"
            #image = AsyncImage(source=src, allow_stretch=True)
            #carousel.add_widget(image)
        #return carousel
        #anm=Animation(x=300,y=0)
        #right = Carousel()
        btn=Button(text="Move",on_press=self.c_animation)
        self.img = Image(source='figure-right.gif')

        #carousel.add_widget(img)
        self.img.add_widget(btn)
        #right.add_widget(img)

        return self.img
        #return right
        #return Button(text="Test")

if __name__=="__main__":
    PitonApp().run()